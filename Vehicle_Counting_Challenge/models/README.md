# Models

This folder is intentionally lightweight.

The default configuration references `yolo11n.pt`. When the project runs for the first time, Ultralytics downloads the weight file automatically if it is not already present.

You may also place exported artifacts here:

- `.pt`
- `.onnx`
- OpenVINO directories
